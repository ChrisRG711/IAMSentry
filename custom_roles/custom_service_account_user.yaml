# Custom Service Account User Role
# Replaces: roles/iam.serviceAccountUser (5 permissions -> ~3 permissions)
# Purpose: Minimal service account impersonation

title: "Custom Service Account User"
description: "Minimal service account impersonation permissions"
stage: "GA"
includedPermissions:
  # Service account usage
  - iam.serviceAccounts.actAs
  - iam.serviceAccounts.get
  
  # Basic project access
  - resourcemanager.projects.get