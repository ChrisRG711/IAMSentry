# IAMSentry Dependencies
# ====================
#
# Install with: pip install -r requirements.txt
#
# For development: pip install -r requirements-dev.txt
# For dashboard: pip install -r requirements.txt && pip install fastapi uvicorn aiofiles jinja2

# Google Cloud Authentication
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Google Cloud APIs
google-api-python-client>=2.0.0

# Google Cloud Secret Manager (for secure credential management)
google-cloud-secret-manager>=2.0.0

# Configuration
PyYAML>=6.0
pydantic>=2.0.0

# Scheduling
schedule>=1.0.0

# HTTP client (used by google-api-python-client)
httplib2>=0.20.0

# CLI and terminal formatting
typer>=0.9.0
rich>=13.0.0

# Security - password hashing (strongly recommended for dashboard auth)
passlib[bcrypt]>=1.7.0

# Monitoring (optional - for /metrics endpoint)
# prometheus-client>=0.17.0
